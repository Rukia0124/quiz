# Backend of quiz project

## Starting

### install dependencies

you should run `npm i` to install dependencies

### init configuration

create a file `.env.developpement` with values :
```
SECRET_TOKEN="secret"
DB_LINK="mongodb+srv://user:pass@host/quiz"
```

### Launch application

`nodemon server`